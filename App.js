import { WebView } from 'react-native-webview';

export default _ => <WebView source={{ uri: 'http://10.0.2.2:35349/Yield' }} />;
// export default _ => <WebView source={{ uri: 'https://yieldly.netlify.app' }} />;
